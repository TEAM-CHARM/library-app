import React from 'react';

const Navbar = () => {
  return (
    <nav className='navbar'>
      <div>
        <a href="#">Home</a>
        <a href="#">Pages</a>
        <a href="#">Shop</a>
        <a href="#">Blog</a>
        <button>Get Started</button>
      </div>
    </nav>
  );
};

export default Navbar;
